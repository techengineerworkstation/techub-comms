# API base URL for the backend server.
# Leave empty for local dev (Vite proxy handles /api routing).
# For production, set to the deployed server URL.
VITE_API_URL=

# Vonage client-side API key (from Vonage Dashboard > Applications)
VITE_VONAGE_API_KEY=
